tables:
  - name: product
    rows: 1000
    columns:
      - name: id
        value: ${uuid}
      - name: name
        value: ${product_name}
      - name: price
        range: float
        props:
          min: 10
          max: 50